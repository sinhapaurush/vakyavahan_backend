/**
 * @author Paurush Sinha
 * @param authtoken Authtoken (string).
 * @description Takes authtoken and returns related available socket ID.
 */
export default function fetchSocketId(authtoken: string): string | null {
  return null;
}
